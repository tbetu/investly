// Metro configuration for Expo SDK 54
// Uses the default Expo metro config

const { getDefaultConfig } = require('expo/metro-config');

const config = getDefaultConfig(__dirname);

module.exports = config;
